The broschure images are stored here.
The database will store the path to the wanted image.
We need to implement an incremental variable or something
So that we can store the data. 

same for products. 